tags: [Method]
operationId: demo.simple.ops.hello.get_it
summary: 测试get请求
parameters:
  - name: name
    in: query
    type: string
    default: 'john'
responses:
  200:
    description: Return persons
