swagger: '2.0'
info:
  title: FALSY SIMPLE DEMO API v2
  version: "0.2"
consumes:
  - application/json
produces:
  - application/json
basePath: "/v2/ez"
paths:
  '/hello':
    get:
      tags: [Method]
      operationId: demo.multi.ops.hello.get_it2
      summary: 测试get请求
      parameters:
        - name: name
          in: query
          type: string
          default: 'john'
      responses:
        200:
          description: Return response
