swagger: '2.0'
info:
  title: FALSY SIMPLE DEMO API
  version: "0.1"
consumes:
  - application/json
produces:
  - application/json
beforeId: demo.global_ba.ops.hello.before_all
afterId: demo.global_ba.ops.hello.after_all
basePath: "/v1"
paths:
  '/hello':
    get:
      tags: [Method]
      operationId: demo.global_ba.ops.hello.get_it
      summary: 测试get请求
      parameters:
        - name: name
          in: query
          type: string
          default: 'john'
      responses:
        200:
          description: Return response
    post:
      tags: [Method]
      operationId: demo.global_ba.ops.hello.post_it
      summary: 测试post请求
      parameters:
        - name: name
          in: query
          type: string
          default: 'john'
      responses:
        200:
          description: Return response
    delete:
      tags: [Method]
      operationId: demo.global_ba.ops.hello.delete_it
      summary: 测试delete请求
      parameters:
        - name: name
          in: query
          type: string
          default: 'john'
      responses:
        200:
          description: Return response
    put:
      tags: [Method]
      operationId: demo.global_ba.ops.hello.put_it
      summary: 测试put请求
      parameters:
        - name: name
          in: query
          type: string
          default: 'john'
      responses:
        200:
          description: Return response
    patch:
      tags: [Method]
      operationId: demo.global_ba.ops.hello.patch_it
      summary: 测试patch请求
      parameters:
        - name: name
          in: query
          type: string
          default: 'john'
      responses:
        200:
          description: Return response
